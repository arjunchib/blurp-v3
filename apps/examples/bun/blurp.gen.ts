export interface Add {
  options: {
    /** first operand */
    a: number,
    /** second operand */
    b: number,
  };
}
import { default as $0 } from "./schema/add.schema";
export const schema = { $0 };
